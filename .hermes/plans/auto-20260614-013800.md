# Plan — auto-20260614-013800

## Type
BUGFIX (B안: P2~P4 코드 패치만, P0 secrets + P1 billing은 사용자가 GitHub UI 처리)

## Root cause (from run #27479654667)
- VERCEL_TOKEN 시크릿 부재 → vercel CLI `--token=` null → exit 1 → cascade git exit 128
- 근본은 사용자가 GitHub UI에서 VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID 등록 필요
- 인접 발견: release-gate.yml SC-010 cardinality = 13 인데 실제 MCP tools = 14 (불일치)
- 인접 발견: SC-013..SC-012 주석 오타 (291라인)
- 인접 발견: Node 20 deprecation 경고 (workflow 8개 모두 영향)

## Scope (P2~P4)
1. **P2 — Node 20 → Node 24 옵트인** (workflow 8개 일괄)
   - vercel-prod.yml, vercel-preview.yml, web-ci.yml, release-gate.yml
   - python-worker-ci.yml, codeql.yml, fly-worker-deploy.yml, codeql
   - 모든 workflow에 env `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"` 추가
2. **P3 — vercel token env 처리** (best practice)
   - vercel-prod.yml, vercel-preview.yml
   - inline `--token=${{ secrets.VERCEL_TOKEN }}` → env: VERCEL_TOKEN
   - 명령에서 `--token=$VERCEL_TOKEN` 형태로 변경 (로그 마스킹 + fly 패턴 따름)
3. **P4 — release-gate.yml 정합성**
   - L200, L203, L220, L221, L224: "13" → "14" (cardinality)
   - L291: `# SC-013..SC-012` → `# SC-011..SC-012` (주석 오타)
4. **.env.example** — VERCEL 섹션 추가 (사용자가 시크릿 등록할 때 참조)
   - VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID (모두 placeholder)

## Out of scope (사용자 처리)
- P0 — VERCEL_TOKEN / VERCEL_ORG_ID / VERCEL_PROJECT_ID 시크릿 실제 등록
- P1 — GitHub account billing lock 해제 (opencode가 SESS-006에서 진행 중)
- shpiment/ staged 변경 (별도 처리)

## Work items
- W1. .github/workflows/vercel-prod.yml — token env + Node 24 opt-in
- W2. .github/workflows/vercel-preview.yml — token env + Node 24 opt-in
- W3. .github/workflows/web-ci.yml — Node 24 opt-in (env에)
- W4. .github/workflows/release-gate.yml — Node 24 opt-in + cardinality 13→14 + SC 주석 typo
- W5. .github/workflows/python-worker-ci.yml — Node 24 opt-in
- W6. .github/workflows/fly-worker-deploy.yml — Node 24 opt-in
- W7. .github/workflows/codeql.yml — Node 24 opt-in
- W8. .env.example — VERCEL 섹션 추가

## Verification
- YAML lint: GitHub Actions는 YAML validate를 push 시 자동
- 카디널리티 검증: `find apps/mcp-server/src/tools -maxdepth 1 -type f -name "*.ts" ! -name "index.ts" | wc -l` = 14
- 시크릿 누락 시 run 실패 메시지 명확화: VERCEL_TOKEN env로 옮기면 `vercel pull: error: You defined "--token", but it's missing a value` 형태 → env에서 빠지면 다른 에러 → 빠른 진단 가능

## Branch
- 작업 브랜치: `claude/ci-bugfix-20260614`
- ledger: `auto-20260614-013800`
